---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: "pki-kube-state-metrics-client"
spec:
  commonName: "system:kube-state-metrics"
  secretName: "pki-kube-state-metrics"
  duration: 8760h # 365d
  renewBefore: 4380h # 178d
  subject:
    organizations:
    - "cluster-2"
  usages:
  - "signing"
  - "key encipherment"
  - "client auth"
  - "server auth"
  issuerRef:
    name: "issuer"
    kind: Issuer